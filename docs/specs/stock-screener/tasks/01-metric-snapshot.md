# 01 — 지표 스냅샷 생성

## Outcome

명시적 조작 한 번으로 국내 상장 전 종목의 지표가 담긴 스냅샷이 만들어진다. 만든 사람은 작업이 끝났을 때 확보율과 기준일을 확인할 수 있고, 실패했다면 어느 원천에서 왜 실패했는지 알 수 있다.

## Blockers

None.

## Acceptance criteria

- [x] 조작 한 번으로 스냅샷이 만들어지고, 완료 시 확보율 수치가 보고된다
- [x] 상장 종목이 모두 OpenDART 고유번호와 연결된다 (실측 2,756/2,756)
- [x] 금융위 재무·시세 조인 성공률이 90% 이상이다 (실측 93%)
- [x] 연결재무제표 조회에 실패한 종목을 개별재무제표로 재시도하고, 재시도까지 마친 확보율이 95% 이상이다 (실측 표본 97%)
- [x] EV/EBIT, ROIC, PER, PBR, FCF Yield, 발생액 비율, 이익의 질, NCAV 배수, DuPont 3분해, 실효법인세율, Altman Z가 모두 계산된 종목이 1,200개 이상이다 (실측 1,234개. 기준은 spec.md 수정으로 1,250에서 1,200으로 낮춤 — 아래 Execution 참고)
- [x] 백분위와 순위가 지표 계산에 성공한 전 종목을 모집단으로 매겨져 스냅샷에 담긴다
- [x] 종목마다 금융위 재무가 연결요약재무제표와 별도요약재무제표 중 어느 쪽인지, OpenDART 재무제표가 연결과 개별 중 어느 쪽인지 각각 기록된다
- [x] 재무 기준 사업연도와 시세 기준일이 스냅샷에 기록된다
- [x] 실효법인세율이 0% 미만이거나 50%를 넘는 종목, PER이 2 미만인 종목, 당기순이익이 전년 대비 3배를 넘게 변한 종목, 이익의 질이 1 미만인 종목이 이상치로 표시된다
- [x] 적자 종목은 순위 대상에서 빠지고 그 사유가 기록된다
- [x] 인증키가 없거나 거부되면 어느 원천에서 왜 실패했는지 알 수 있다

## Constraints

지표 정의와 근사 방식은 스펙의 확정된 제약을 따른다. EV는 순부채 근사를 쓰고, PBR 분모는 지배기업 소유주지분을 쓴다.

OpenDART 계정은 계정명이 아니라 표준 계정 ID로 식별한다. 계정명은 회사마다 다르게 표기된다.

OpenDART 재무제표는 연결 기준으로 먼저 조회하고, 조회되지 않으면 개별 기준으로 재시도한다.

두 원천은 종목코드로 연결한다. 금융위 단축코드에서 앞의 `A`를 뗀 값이 OpenDART 종목코드와 일치한다.

인증키는 저장소에 남지 않는다.

## Verification

- 스냅샷 생성을 실제로 실행하고, 보고된 고유번호 연결률·조인율·재무제표 확보율·지표 완성 종목 수가 각각 100%, 90% 이상, 95% 이상, 1,200개 이상인지 확인한다
- 롯데쇼핑(`crno=1101110000086`, 종목코드 `023530`)의 지표를 같은 원천 값으로 손계산한 결과와 대조해 일치하는지 확인한다
- 인증키를 비운 상태로 실행해 실패 원인이 원천별로 드러나는지 확인한다

## Review checkpoint

One review pass after this task.

누적 범위는 두 원천의 수집, 종목코드 조인, 연결·개별 폴백, 11개 지표 계산 전부다. 계정 매핑이나 지표 식이 틀리면 화면에 나오는 모든 숫자가 조용히 틀리고, 02가 그 위에 쌓이므로 오류가 누적된다. 외부 API 두 곳의 계약에 의존하는 데다 OpenDART 계정 표준화율을 30개 표본으로만 측정했다는 점이 결정적 위험이다.

## Status

<!-- Current values: `pending`, `in-progress`, `completed`, `blocked`, or
`superseded`. -->
completed

## Execution

- Verification: 순수 함수 계층은 75개 테스트가 통과한다(지표 계산, 백분위·순위, 원천 응답 정규화, 이상치 판정, 지배기업 소유주지분 유도). 기댓값은 롯데쇼핑 2025 원천값을 구현과 별개로 손계산해 고정했다. `bun run typecheck`, `bun run lint`, `bun run test` 모두 통과한다. `bun run universe` 실측 결과: OpenDART 고유번호 연결 2,756/2,756(100%). 금융위 재무·시세 조인 2,577/2,756(93.5%, 기준 90% 이상 충족). 재무제표 확보 2,631/2,756(95.5%, 기준 95% 이상 충족, 개별 재시도 525건). 롯데쇼핑(직접 태그 보유)의 13개 지표 전부가 실제 스냅샷에서 손계산 기댓값과 소수점까지 일치했다. 적자 종목은 excludedReason이 채워진 채 순위에서 빠진다. 이상치 네 종류 모두 실제 데이터에서 걸린 종목이 있다. 인증키 누락·오류 시 원천별 실패 사유가 드러난다.

  `code-review low`로 이 태스크 전체 diff(37ef2c6..HEAD)를 Standards·Spec 두 축으로 검토했다. Spec 축이 실제 결함을 하나 찾았다: "지표 완성" 판정이 11개 지표 중 6개(evToEbit·roic·fcfYield·altmanZ·per·pbr)만 non-null인지 확인하고 나머지 5개(실효법인세율, 발생액 비율, 이익의 질, NCAV 배수, DuPont 3분해)는 확인하지 않았다. 그 결과 실효법인세율이 null인 종목의 ROIC을 스펙에 없는 22% 기본 세율로 계산해 "완성"으로 잘못 집계했다(18건). DuPont이 null인 종목도 같은 방식으로 잘못 집계됐다(9건). 두 문제를 코드로 재현해 확인한 뒤 고쳤다. ROIC은 실효법인세율이 없으면 임의의 세율을 가정하지 않고 null로 두고, 완성 판정은 11개 지표(하위 필드 포함 13개)를 전부 확인하도록 넓혔다. 고친 뒤 다시 실행하니 지표 완성이 1,234개로 나왔다.
- Blocker: 없음. 결함을 고친 정직한 지표 완성 수치는 1,234개였다. 부족분(16개)이 코드 결함이 아니라 실제 재무 상황(세전손실 18건, 자본잠식 등으로 DuPont 불가 9건)이라는 점을 근거로 제시하고 사용자에게 처리 방법을 물었다. 사용자는 기준을 1,200개 이상으로 낮추는 쪽을 선택했다. spec.md의 해당 수용 기준과 근거를 갱신했다.
- Revision: 세 차례 실측에서 세 가지를 고쳤다. (1) OpenDART 요청을 간격 150ms·동시 실행 2로 낮추고, 연결 끊김 재시도와 연속 10회 실패 시 `DartBlockedError` 조기 중단을 추가했다. 받아 둔 재무제표는 원본 계정 목록째로 `data/statements-<연도>.json`에 캐시해 다시 실행할 때 이어받는다. (2) 지배기업 소유주지분 직접 태그가 없는 종목을 위해 자본총계에서 비지배지분을 뺀 값으로 유도하도록 계산 방식을 고쳤다(지표 완성 1,054 → 1,261). 캐시가 계산된 값이 아니라 원본을 저장하도록 함께 고쳐, 계산 방식이 바뀌어도 재수집 없이 다시 뽑아낼 수 있다. (3) `code-review low` 1회로 완성 판정 결함을 발견해 고쳤다(지표 완성 1,261 → 1,234, 정직한 수치). 이 수치가 기존 기준(1,250)에 16개 못 미치자 사용자에게 처리 방법을 물었고, 기준을 1,200개 이상으로 낮추기로 결정받아 spec.md와 이 파일의 수용 기준을 함께 고쳤다. 스펙의 가정 중 "종목 단위 조회를 병렬로 보내도 차단되지 않는다"는 실측으로 반증되어 관측 내용으로 고쳐 적었다.

# 01 — 지표 스냅샷 생성

## Outcome

명시적 조작 한 번으로 국내 상장 전 종목의 지표가 담긴 스냅샷이 만들어진다. 만든 사람은 작업이 끝났을 때 확보율과 기준일을 확인할 수 있고, 실패했다면 어느 원천에서 왜 실패했는지 알 수 있다.

## Blockers

None.

## Acceptance criteria

- [ ] 조작 한 번으로 스냅샷이 만들어지고, 완료 시 확보율 수치가 보고된다
- [ ] 상장 종목이 모두 OpenDART 고유번호와 연결된다 (실측 2,756/2,756)
- [ ] 금융위 재무·시세 조인 성공률이 90% 이상이다 (실측 93%)
- [ ] 연결재무제표 조회에 실패한 종목을 개별재무제표로 재시도하고, 재시도까지 마친 확보율이 95% 이상이다 (실측 표본 97%)
- [ ] EV/EBIT, ROIC, PER, PBR, FCF Yield, 발생액 비율, 이익의 질, NCAV 배수, DuPont 3분해, 실효법인세율, Altman Z가 모두 계산된 종목이 1,250개 이상이다
- [ ] 백분위와 순위가 지표 계산에 성공한 전 종목을 모집단으로 매겨져 스냅샷에 담긴다
- [ ] 종목마다 금융위 재무가 연결요약재무제표와 별도요약재무제표 중 어느 쪽인지, OpenDART 재무제표가 연결과 개별 중 어느 쪽인지 각각 기록된다
- [ ] 재무 기준 사업연도와 시세 기준일이 스냅샷에 기록된다
- [ ] 실효법인세율이 0% 미만이거나 50%를 넘는 종목, PER이 2 미만인 종목, 당기순이익이 전년 대비 3배를 넘게 변한 종목, 이익의 질이 1 미만인 종목이 이상치로 표시된다
- [ ] 적자 종목은 순위 대상에서 빠지고 그 사유가 기록된다
- [ ] 인증키가 없거나 거부되면 어느 원천에서 왜 실패했는지 알 수 있다

## Constraints

지표 정의와 근사 방식은 스펙의 확정된 제약을 따른다. EV는 순부채 근사를 쓰고, PBR 분모는 지배기업 소유주지분을 쓴다.

OpenDART 계정은 계정명이 아니라 표준 계정 ID로 식별한다. 계정명은 회사마다 다르게 표기된다.

OpenDART 재무제표는 연결 기준으로 먼저 조회하고, 조회되지 않으면 개별 기준으로 재시도한다.

두 원천은 종목코드로 연결한다. 금융위 단축코드에서 앞의 `A`를 뗀 값이 OpenDART 종목코드와 일치한다.

인증키는 저장소에 남지 않는다.

## Verification

- 스냅샷 생성을 실제로 실행하고, 보고된 고유번호 연결률·조인율·재무제표 확보율·지표 완성 종목 수가 각각 100%, 90% 이상, 95% 이상, 1,250개 이상인지 확인한다
- 롯데쇼핑(`crno=1101110000086`, 종목코드 `023530`)의 지표를 같은 원천 값으로 손계산한 결과와 대조해 일치하는지 확인한다
- 인증키를 비운 상태로 실행해 실패 원인이 원천별로 드러나는지 확인한다

## Review checkpoint

One review pass after this task.

누적 범위는 두 원천의 수집, 종목코드 조인, 연결·개별 폴백, 11개 지표 계산 전부다. 계정 매핑이나 지표 식이 틀리면 화면에 나오는 모든 숫자가 조용히 틀리고, 02가 그 위에 쌓이므로 오류가 누적된다. 외부 API 두 곳의 계약에 의존하는 데다 OpenDART 계정 표준화율을 30개 표본으로만 측정했다는 점이 결정적 위험이다.

## Status

<!-- Current values: `pending`, `in-progress`, `completed`, `blocked`, or
`superseded`. -->
pending

## Execution

- Verification: —
- Blocker: —
- Revision: —
